package com.tpadsz.servlet.design.pattern.decorator;

/**
 *  The top abstract where concrete component and decorator
 *  should be derived from
 */
public interface Component  {
     void PrintString(String s);
}